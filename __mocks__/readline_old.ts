// import readline2 from 'readline';

// export const onFn = jest.fn().mockImplementation((evt, cb) => {
//   cb('test');
// }).mockName('on');

// const readline = {
//   createInterface: jest.fn().mockImplementation(() => {
//     return {on: onFn};
//   }),
// };

// export default readline;